## TW STOCK ML PREDICTOR
---
```
usage: run.py [-h] [--init] [--start] [--train]

optional arguments:
  -h, --help  show this help message and exit
  --init      initialize database
  --start     start web serivce
  --train     train new model based on current dataset
```